apiVersion: devices.kubeedge.io/v1alpha2
kind: DeviceModel
metadata:
  name: testdevice-model
  namespace: default
spec:
  properties:
    - name: testdevice-status
      description: testdevice-status
      type:
        string:
          accessMode: ReadOnly
          defaultValue: ''